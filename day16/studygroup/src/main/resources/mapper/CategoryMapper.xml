<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pknu26.studygroup.mapper.CategoryMapper">

    <!-- resultMap을 아래의 CRUD xml에서 resultType 대신 사용하고 싶으면 아래와 같이 작성하고 사용 -->
    <resultMap id="categoryResultMap" type="com.pknu26.studygroup.dto.Category">
        <id property="categoryId" column="category_id" />
        <result property="categoryName" column="category_name" />
    </resultMap>

    <select id="findAll" resultType="Category">
        SELECT category_id
             , category_name
          FROM categories
         ORDER BY category_id ASC
    </select>

    <select id="findById" parameterType="long" resultType="Category">
        SELECT category_id
             , category_name
         FROM categories
        WHERE category_id = #{categoryId}
    </select>

    <select id="countByName" parameterType="string" resultType="int">
        SELECT COUNT(*)
          FROM categories
         WHERE category_name = #{categoryName}
    </select>

    <insert id="insertCategory" parameterType="Category">
        INSERT INTO categories (
            category_id,
            category_name
        ) VALUES (
            seq_categories.NEXTVAL,
            #{categoryName}
        )
    </insert>

    <update id="updateCategory" parameterType="Category">
        UPDATE categories
           SET category_name = #{categoryName}
         WHERE category_id = #{categoryId}
    </update>

    <delete id="deleteCategory" parameterType="long">
        DELETE FROM categories
         WHERE category_id = #{categoryId}
    </delete>
</mapper>
